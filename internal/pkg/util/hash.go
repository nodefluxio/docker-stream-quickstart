package util

import "golang.org/x/crypto/bcrypt"

// HashPassword hash string for password usage
func HashPassword(password string) (string, error) {
	bytes, err := bcrypt.GenerateFromPassword([]byte(password), 14)
	if err != nil {
		return "", err
	}
	return string(bytes), nil
}
